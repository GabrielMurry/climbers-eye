import React from "react";
import Header from "../common/header/Header";
import BackIcon from "../common/header/BackIcon";

const CreateSpraywallHeader = () => {
  return <Header leftIcon={<BackIcon />} centerText="Create Spray Wall" />;
};

export default CreateSpraywallHeader;
